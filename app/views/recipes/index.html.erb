<h2>Your recipes:</h2>
<% @your_recipes.each do |recipe| %>
    <%= "#{recipe.name} by you" %> 
<% end %><br>

<h2>Public recipes:</h2>
<% @public_recipes.each do |recipe| %>
    <%= link_to recipe.name, recipe_path(recipe) %> by <%= link_to recipe.user.name, user_path(recipe.user) %>
<% end %>