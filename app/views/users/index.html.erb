<h2>Users:</h2>
<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to user.name, user_path(user) %> has <%= pluralize(user.recipes.length, 'recipe') %>
        </li>
    <% end %>
</ul>
<br>
Create new items when you make a new recipe or list!
<% if !session[:user_id] %>
    <br><%= link_to "Sign up or Log in", login_path %>
<% end %>